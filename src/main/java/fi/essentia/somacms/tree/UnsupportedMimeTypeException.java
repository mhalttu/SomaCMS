package fi.essentia.somacms.tree;

/**
 * Thrown e.g. if we try to open or create a text document that isn't recognized as one
 */
public class UnsupportedMimeTypeException extends RuntimeException {
}
